# Gestor PyME - Variables de Entorno
# Copia este archivo como .env y configura tus valores

# SEGURIDAD: Django Secret Key
# Genera una nueva con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-CAMBIA-ESTE-VALOR-POR-UNO-NUEVO

# MODO DEBUG
# True para desarrollo, False para producción
DEBUG=True

# HOSTS PERMITIDOS
# En producción, separar múltiples hosts con comas: midominio.com,www.midominio.com
ALLOWED_HOSTS=localhost,127.0.0.1

# BASE DE DATOS
# Para desarrollo (SQLite):
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Para producción (PostgreSQL):
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=nombre_base_datos
# DATABASE_USER=usuario
# DATABASE_PASSWORD=contraseña
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
